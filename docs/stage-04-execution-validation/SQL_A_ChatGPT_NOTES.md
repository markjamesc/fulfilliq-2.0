- **Scope:** SQL A implements only the supplied `Stage_03_Measurement_Design(1).md` v0.2.1 and `00_STAGE4_SHARED_PACKET.md`. Membership thresholds remain provisional. Outcomes are descriptive, retrospective recommendations; handoff evidence does not establish seller fault or intervention effectiveness.

- **Before execution:** Populate the script's snapshot ID, source version, outcome-observation boundary, and temporal convention; set the verification flag only after checking the frozen source and mappings. These facts were not supplied. The implementation requires the five temporal columns to be native `DATETIME` at one recorded precision and the three source tables to be InnoDB. Text, date-only, or `TIMESTAMP` mappings require verified adaptation; the script halts instead of guessing. No timezone conversion is applied. Identity validity means non-null, nonblank identifiers; identities are neither trimmed nor case-normalized for joins. Joins, uniqueness checks, and seller tie-breaking use original bytes.

- **Execution:** Use one dedicated MySQL 8.0 connection with SELECT, CREATE TEMPORARY TABLES, CREATE ROUTINE, EXECUTE, and ALTER ROUTINE privileges. Stop on the first error. The script creates its named procedure without overwriting an existing routine, calls it, and drops it after success. Source tables are unchanged. Result sets are the complete judged seller table, source/cohort audit, and actual schema metadata. Temporary `a4_evidence` and `a4_judged` remain available in that connection. After an error, close the dedicated connection and remove this script's procedure before retrying. Schema metadata supports verification; no absence of featured/catalog fields is asserted.

- **Field conventions:** Unsuffixed outcomes and gates use DATE lateness; `_timestamp` fields retain the timestamp run's evidence. Counts remain exact integer-valued decimals; rate and burden quotients are display-only. Zero eligible volume retains the count pair `0/0` and NULL LFR. Rank uses rational cross-products and bytewise ties. The seller-level `multi_seller_order_flag` means at least one eligible multi-seller association. As required by Section 20, `single_seller_association_n` is the complement of the multi-seller count; `eligible_n_ss` instead requires exactly one valid seller and no unresolved seller association. Nonblank unresolved seller identifiers contribute to the distinct-association count. Unclassified associations are reported separately and cannot supply single-seller evidence.

- **N4/N5:** Both N4 branch flags are retained. When both fire, the single branch label is `thin_majority`; otherwise it is `flip` or `none`. N5 compares provisional actions after selection, removes disagreers, and refills once using the original DATE comparator and ranking rule. Following Section 17.5's specific interaction rule, clock-action disagreement takes primary-reason precedence; DATE gate failures and both runs' N4 flags remain visible.

- **Capacity and release:** C=S=20; O=R=0 are simulation assumptions, never source columns or operational facts. Every row carries the required simulation label and has operational-release authorization set to zero. Source-key, orphan, or purchase-date anomalies receive a release hold because their effects have not been bounded in the supplied material; a documented hold can also be supplied. Holds preserve calculated decisions. Source-audit categories can overlap; delivery exclusion counts are mutually exclusive in the locked precedence order.

- **Validation:** All 14 relational statements were parsed as MySQL and exercised on synthetic data through a dialect-adapted local engine. Checks covered zero volume, item collapse, coverage equality, half partitions, handoff nesting, exact rational ranking, N4 branches and unusable comparators, N5 removal/refill, and the capacity invariant. This is not native MySQL execution or validation against the actual `fulfilliq` data; procedure execution and live source verification remain outstanding.
